rate limiting these path (not limited to): bookings, change password,...  

Date object: fix mismatch between server and client, and especially in Nextjs.

sign up otp: handle resend otp (limit times, after e.g. 3 mins) and expriration of OTP.

TODO: payment result page.

after booking: block user to back to the payment success (only show for a while).
TODO: on searching: instead of joining room, go check the inventory

// TODO: search bar: get the default from the section top destination.