filter: onsubmit need to somehow capture the search spec to call server action, or set searchparams
  then router.push.

filter: make it sticky on large screen.

proxy: handle redirecting.

types: unified them into one place, such as "types" folder.
search bar: ambiguity on naming input/output/value of zod types.

consider? make buckets for materialized view in search query: bucket for (province or district) & (3d | 7d | 15d | 30d) & (2guest | ... commonly search number of guests)

search bar -> search page -> result: too bloated on tossing name prop around.

homepage: favorite destination -> real link

hotel owner dashboard: make meaningful analytical charts.

rate limiting these path (not limited to): bookings, change password,...  

review's pagination: doesn't work

seeding: booking: incrementally, bulk on each search.

Date object: fix mismatch between server and client, and especially in Nextjs.


// TODO: Flow
// 1. Basic info (name, email, phone) -> sign up for account
// 1.1. verify email with OTP -> after verification, we can create the account and sign in the user immediately
// 2. auto sign in -> server sends jwt, create a session id, redirect to that id (timed session).
// 3. After sign in, show partnership application form (business details, tax info, etc) -> submit to server
// 3.1. if user exit without submitting, they can come back to the form and continue where they left off (fetch saved data from server)
// but if the session is expired, they need to sign in again first, then we can give another session id and fetch the saved form data with that session id.